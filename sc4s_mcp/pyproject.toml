[tool.poetry]
name = "sc4s-mcp"
version = "0.1.0"
description = "MCP server for SC4S"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
fastmcp = "*"
httpx = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

# ===============================================================================================
# Juniper Structured logging
# ===============================================================================================


@module confgen context(log) name(gen_log_rfc_5424_strict-juniper_junos) exec("gomplate --file `syslog-ng-sysconfdir`/conf.d/log_paths/p_rfc_5424_strict-juniper_junos.conf.tmpl")

log {
    #Default source
    gen_log_rfc_5424_strict-juniper_junos(
        sourcedefault(yes)
    );

};
log {
    #Default source
    gen_log_rfc_5424_strict-juniper_junos(
        sourcedefault(no)
    );

};


# ===============================================================================================
# Fortigate FortiOS
# ===============================================================================================

@module confgen context(log) name(gen_log_rfc_3164-fortigate-fortios) exec("gomplate --file `syslog-ng-sysconfdir`/conf.d/log_paths/p_rfc3164-fortinet_fortios.conf.tmpl")

log {
    #Default source
    gen_log_rfc_3164-fortigate-fortios(
        sourcedefault(yes)
    );

};

log {
    #Default source
    gen_log_rfc_3164-fortigate-fortios(
        sourcedefault(no)
    );

};

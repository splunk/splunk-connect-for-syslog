block parser p_add_context_splunk_tags(key("none")) {
    add-contextual-data(
        selector("`key`"),
        database("context/splunk_tag.csv")
    );
};

filter f_rfc_3164_program {
    not program("[A-Za-z0-9_\-]{1,32}");
};
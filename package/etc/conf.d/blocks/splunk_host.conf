block parser p_add_context_splunk_host() {
    add-contextual-data(
        selector(filters("/opt/syslog-ng/etc/context/filter_host.conf")),
        database("conf.d/blocks/splunk_host.csv")
        ignore-case(yes)
        prefix(".metadata.")
    );
};


block parser vendor_product_by_source() {
    add-contextual-data(
        selector(filters("/opt/syslog-ng/etc/context/vendor_product_by_source.conf")),
        database("conf.d/blocks/_common/vendor_product_by_source.csv")
        ignore-case(yes)
        prefix("fields.")
    );
};


block parser p_add_context_splunk(sourcetype("syslogng-fallback")) {
    add-contextual-data(
        selector("`sourcetype`"),
        database("context/splunk_index.csv"),
        prefix(".context.")
    );
};
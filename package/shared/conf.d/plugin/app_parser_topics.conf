
parser app-group-sc4s-almost-syslog{
    app-parser(topic(sc4s-almost-syslog));
};

parser app-group-sc4s-syslog-sdata{
    app-parser(topic(sc4s-syslog-sdata));
};
parser app-group-sc4s-syslog-pgm{
    app-parser(topic(sc4s-syslog-pgm));
};
parser app-group-sc4s-syslog{
    app-parser(topic(sc4s-syslog));
};

parser app-group-sc4s-syslog-netsource{
    app-parser(topic(sc4s-network-source));
};

parser app-group-sc4s-raw{
    app-parser(topic(sc4s-raw-syslog));
};
parser app-group-sc4s-fallback{
    app-parser(topic(fallback));
};

parser app-plugin-syslog-fix-program{
    app-parser(topic(fix-invalid-program));
};

parser app-plugin-source-postprocess{
    app-parser(topic(sc4s-postfilter));
};

parser app-plugin-source-finalprocess{
    app-parser(topic(sc4s-finalfilter));
};